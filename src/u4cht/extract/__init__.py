"""各類原始資料字串抽取器。

計畫子模組（尚未實作）：

- `tlk`      — 從 DOS .TLK 抽 256 NPC 對話（對應上游 `tools/extract_tlk.py`）
- `strings`  — 從 title.exe / avatar.exe 抽 stringtable（對應 `extract_stringtable.py`）
- `hardcoded`— 靜態分析 xu4 C++ 內 `screenMessage(...)` 字面（對應 `extract_hardcoded.py`）
- `vendor`   — 從 `module/Ultima-IV/vendors.b` Boron 腳本抽字（對應 `extract_vendor_boron.py`）
"""
