// M1 emscripten hello-world spike
// 只是為了驗證 docker + emcc toolchain 通，能產出 .wasm + .html
#include <stdio.h>
int main() {
    printf("Hello from xu4-cht-python Phase A1 emscripten spike!\n");
    return 0;
}
